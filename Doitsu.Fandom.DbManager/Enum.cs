using System;
using System.Collections.Generic;
using System.Text;

namespace Doitsu.DBManager.Fandom
{

    /// <summary>
    /// This enum to support show to view
    /// Artist is the mark to mapping albums to artist detail
    /// Present is the mark to mapping albums to home 
    /// </summary>
    public enum ProductCollectionTypeEnum
    {
        Artist = 1,
        Present = 2
    }

    public enum SliderTypeEnum
    {
        Blog = 1,
        ProductCollection = 2
    }
}
