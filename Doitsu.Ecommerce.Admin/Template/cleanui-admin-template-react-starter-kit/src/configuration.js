const isProduction = process.env.NODE_ENV === 'production';

const config = {
    // "baseAPIUrl": "http://ygfl.api.doitsu.tech/api/"
    "baseAPIUrl": "http://localhost:10000/api/"
};

export default config